node_modules/
dist/
.env
*.js.map
data/
.claude/
data/deposit-master.json
data/wallet-master.json

# Database files (contain secrets)
data/agentos.db-wal
data/agentos.db-shm
data/*.db-wal
data/*.db-shm
data/agentos.db
node_modules/

# Internal notes
/internal/

# Discovery page build inputs (source videos + design refs).
# Extracted WebP frames live under public/discovery/frames/ and ARE committed.
/discovery-build/
tmp/

# Ad-hoc test fixtures users drop at repo / cli root when poking commands
# (e.g. `'[...]' | Set-Content thread.json` for `agentos twitter schedule --file`).
/thread*.json
/cli/thread*.json
*.test.json

# TypeScript incremental build cache
*.tsbuildinfo

# OS metadata
.DS_Store
Thumbs.db

# Editor / IDE
*.swp
*~

# Stray logs
*.log
